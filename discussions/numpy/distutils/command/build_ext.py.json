[
  {
    "id" : "f6f71e66-b4d2-4dae-8274-2053ab54be7f",
    "prId" : 5866,
    "prUrl" : null,
    "prSource" : "GitHub",
    "comments" : [
      {
        "id" : "c54c546f-4506-4286-8cb6-5729dc44bda9",
        "parentId" : null,
        "authorId" : "7ff2710f-0a12-46fb-a54a-d405dab78cf7",
        "body" : "Sorry for this late comment, but at the moment we have the odd situation where the code modifies `self.include_dirs` and then replaces it.  Would this not be clearer as:\n\n```\n        # Ensure include dirs and distribution.include_dirs are lists\n        incl_dirs = self.include_dirs or []\n        if isinstance(incl_dirs, str):\n            incl_dirs = incl_dirs.split(os.pathsep)\n        if self.distribution.include_dirs is None:\n            self.distribution.include_dirs = []\n        # Make sure self.include_dirs is same list as self.distribution.include_dirs\n        self.include_dirs = self.distribution.include_dirs\n        self.include_dirs.extend(incl_dirs)\n```\n",
        "createdAt" : "2015-09-18T21:43:23Z",
        "updatedAt" : "2015-09-18T21:43:23Z",
        "lastEditedBy" : "7ff2710f-0a12-46fb-a54a-d405dab78cf7",
        "tags" : [
        ]
      }
    ],
    "commit" : "b08f369554e6d6b231c941740f6c852a45edcc12",
    "line" : 14,
    "diffHunk" : "@@ -1,1 +64,68 @@        # The include paths will be passed to the compiler in the order:\n        # numpy paths, --include-dirs paths, Python include path.\n        if isinstance(self.include_dirs, str):\n            self.include_dirs = self.include_dirs.split(os.pathsep)\n        incl_dirs = self.include_dirs or []"
  }
]